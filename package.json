{
  "scripts": {
    "watch": "lerna run --parallel watch",
    "test": "lerna run test",
    "test:watch": "lerna run --parallel test:watch"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
